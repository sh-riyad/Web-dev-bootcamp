```
1. Start (Production Mode):

      Command: deno task start
      This command runs the server without extra development permissions and features, making it suitable for production use.

2. Dev (Development Mode):

      Command: deno task dev
      This command runs the server in development mode with additional permissions and the --watch flag, allowing the server to restart automatically when changes are detected.

```
